{
  "$id": "https://meta.rdkcentral.com/firebolt/app-manifest/app/legacy",
  "title": "Firebolt App Manifest: App Config: Legacy Config",
  "description": "app.legacy",
  "type": "object",
  "properties": {
    "mode": {
      "description": "Defines if this config is for 'legacy mode' and, if so, for which mode. This is a 'free text' string, but the value should match what a given legacy system expects.",
      "type": "string"
    },
    "config": {
      "description": "The specific configuration data needed for the legacy mode to run the app. Dictionary[string => string].",
      "type": "object",
      "additionalProperties": { "type": "string" }
    }
  },
  "additionalProperties": false,
  "required": [
    "mode", "config"
  ]
}
